import { ConvertImgToUrlPipe } from './convert-img-to-url.pipe';

describe('ConvertImgToUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertImgToUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
